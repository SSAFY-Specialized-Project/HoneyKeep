import {AccountConnectWidget} from "@/widgets/certification/ui";

export const AccountConnect = () => {

    return (
        <div className="h-full">
            <AccountConnectWidget />
        </div>
    );
};

export default AccountConnect;
