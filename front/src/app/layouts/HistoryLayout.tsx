import { Layout } from "@/shared/ui";
import { HistoryHeader } from "@/widgets/header/ui";

const HistoryLayout = () => {
  return <Layout headerSlot={<HistoryHeader />} />;
};

export default HistoryLayout;
